- defaults:
    link:
      relink: true
      create: true

- clean: ['~', '~/.config']

- link:
    ~/.config/xmonad: xmonad
    ~/.config/dunst: dunst
    ~/.config/bottom: bottom
    ~/.zshrc: zsh/.zshrc
